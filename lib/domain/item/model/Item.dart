class Item {
  String productId;
  String name;
  double quantity;
  String priority;
  double maxValue;
  bool status = false;

  Item(this.productId, this.name, this.quantity, this.priority, this.maxValue, this.status);

}
